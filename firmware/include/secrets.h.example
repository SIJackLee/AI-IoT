#pragma once
// Copy to secrets.h and fill in. Do NOT commit secrets.h.

#ifndef WIFI_SSID
#define WIFI_SSID "YOUR_SSID"
#endif

#ifndef WIFI_PASSWORD
#define WIFI_PASSWORD "YOUR_PASSWORD"
#endif

// Firebase Realtime Database host only (no https://)
// e.g. myproj-default-rtdb.asia-southeast1.firebasedatabase.app
#ifndef FIREBASE_HOST
#define FIREBASE_HOST "YOUR_PROJECT-default-rtdb.REGION.firebasedatabase.app"
#endif

// Test mode: leave empty. Production: auth token / DB secret
#ifndef FIREBASE_AUTH
#define FIREBASE_AUTH ""
#endif

#ifndef DEVICE_ID
#define DEVICE_ID "aiot01"
#endif
